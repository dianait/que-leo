# Variables de entorno para Article Reader

# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Authentication Configuration (opcional)
# Si no se especifica, se detecta automáticamente según el entorno
VITE_AUTH_REDIRECT_URL=https://que-leo.vercel.app

# Nota: En desarrollo local (npm run dev), la aplicación usará automáticamente
# la URL actual (localhost:5173) sin necesidad de configurar esta variable. 